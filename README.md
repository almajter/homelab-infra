# homelab-infra

Infrastructure as code for the homelab (Proxmox `pve`, 10.0.10.4).

- `terraform/` creates containers/VMs on pve (bpg/proxmox provider)
- `ansible/` configures what runs inside them

